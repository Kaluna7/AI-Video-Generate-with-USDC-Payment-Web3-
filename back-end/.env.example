# Backend environment variables (example)
# Copy to `back-end/.env` (this file is ignored by git)

# PostgreSQL example:
# DATABASE_URL=postgresql+psycopg2://postgres:YOUR_PASSWORD@localhost:5432/web3

# Or any SQLAlchemy-supported URL:
DATABASE_URL=

# JWT secret (change in production)
JWT_SECRET_KEY=CHANGE_ME_TO_A_RANDOM_SECRET
